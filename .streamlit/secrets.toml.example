# Streamlit secrets — 이 파일은 예시. 실제 사용 시 .streamlit/secrets.toml 으로 복사 후 키 입력
# 또는 Streamlit Cloud 대시보드 → App Settings → Secrets 에 등록 (권장)

# Groq API (primary — Streamlit Cloud 추천)
GROQ_API_KEY = "gsk_..."

# Google AI Studio (선택)
GEMINI_API_KEY = "AIza..."
